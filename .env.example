# VITE_API_BASE: Optional. Base URL of the backend API used by the site.
# Defaults to "/api" which is served by the Vercel serverless functions in
# ./api (backed by ./data/products.json). Only set this when overriding to a
# separate hosted backend (e.g. an external Express deploy).
VITE_API_BASE="/api"